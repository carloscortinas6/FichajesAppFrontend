import React from 'react';

const BotonReporte = ({ onClick }) => {
    return (
        <button onClick={onClick} className="boton-reporte">
            Buscar
        </button>
    );
};

export default BotonReporte;
